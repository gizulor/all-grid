<txp:variable name="random_to" value='<txp:yield name="to" escape="number" />' /><txp:php>
	global $variable;
	echo (rand(1,$variable['random_to']));

	// $n=range(1,$variable['random_to']);
	// shuffle($n);
	// for ($x=0; $x< 10; $x++)
	// {
		// echo $n[$x].' ';
	// }
// echo "\n"
// 
</txp:php>